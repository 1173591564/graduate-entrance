# Android

Jetpack Compose client for daily study tasks and offline-first synchronization.

## Requirements

- JDK 17
- Android SDK 35

## Commands

```bash
./gradlew lint testDebugUnitTest assembleDebug
```

The project includes the Room, Retrofit and WorkManager dependency baseline. Data and sync
implementations will be added with the corresponding business slices.
