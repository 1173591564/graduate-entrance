# Web

Vue 3 administration interface for the 11408 graduate entrance preparation system.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run typecheck
npm run test
npm run build
```

The development server proxies `/api` to `http://localhost:8000`.
