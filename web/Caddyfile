{$SITE_ADDRESS::80} {
	encode zstd gzip

	handle /api/* {
		reverse_proxy backend:8000 {
			header_up Authorization "Bearer {$API_TOKEN}"
		}
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
